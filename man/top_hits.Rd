% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/top-hits.R
\name{top_hits}
\alias{top_hits}
\title{Extract top hits from GWAS results}
\usage{
top_hits(
  data,
  p_threshold = 5e-08,
  clump_window = 1e+06,
  genes = NULL,
  max_gene_distance = 5e+05,
  n = 20
)
}
\arguments{
\item{data}{A \code{gwas_data} object or data.frame.}

\item{p_threshold}{Significance threshold for lead SNPs.}

\item{clump_window}{Window size in bp for clumping. Only the most
significant SNP within each window is retained.}

\item{genes}{Optional gene annotation data.frame (chr, start, end, gene).
If provided, nearest gene is added.}

\item{max_gene_distance}{Maximum distance to assign a gene.}

\item{n}{Maximum number of hits to return.}
}
\value{
A data.frame with columns: SNP, CHR, BP, P, BETA, SE,
nearest_gene (if genes provided), gene_distance, cytoband.
}
\description{
Identifies independent significant loci using window-based clumping,
optionally annotates with nearest gene, and returns a publication-ready
table.
}
\examples{
data(example_gwas)
top_hits(example_gwas, p_threshold = 0.001, n = 10)
}
