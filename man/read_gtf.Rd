% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-gtf.R
\name{read_gtf}
\alias{read_gtf}
\title{Read gene annotations from GTF/GFF3 file}
\usage{
read_gtf(
  path,
  feature_type = "gene",
  gene_name_attr = c("gene_name", "Name", "gene"),
  gene_id_attr = "gene_id",
  biotype = NULL
)
}
\arguments{
\item{path}{Path to GTF or GFF3 file (plain text or gzipped).}

\item{feature_type}{Feature type to extract (column 3 of GTF).
Default \code{"gene"}.}

\item{gene_name_attr}{Attribute key(s) to use as gene name, tried in
order. For GTF: \code{"gene_name"}, for GFF3: \code{"Name"}.}

\item{gene_id_attr}{Attribute key for gene ID (e.g. ENSG...).}

\item{biotype}{Character vector of gene biotypes to keep, e.g.
\code{"protein_coding"}. If NULL (default), all biotypes are returned.}
}
\value{
A data.frame with columns: chr (integer), start, end, gene,
strand, gene_id. Ready for \code{\link[=gene_track]{gene_track()}} or
\code{locus_plot(gene_data = ...)}.
}
\description{
Parse a GTF or GFF3 annotation file and return a data.frame of gene
positions ready for use with \code{\link[=gene_track]{gene_track()}} or \code{\link[=locus_plot]{locus_plot()}}.
Supports \code{.gz} compressed files.
}
\examples{
\dontrun{
# Ensembl GTF
genes <- read_gtf("Homo_sapiens.GRCh38.110.gtf.gz")

# Use with gene_track
gene_track(genes, region_chr = 6, region_start = 25e6, region_end = 35e6)

# GFF3 format
genes <- read_gtf("gencode.v44.annotation.gff3.gz")
}
}
